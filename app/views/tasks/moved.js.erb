<% if @task.errors.any? %>
taskList = $("#<%= @task.status_was.name.capitalize %>");
<% else %>
taskList = $("#<%= @status.name.capitalize %>");
<% end %>
taskElement = $("#<%= @task.id%>");
taskElement.remove();
taskList.append("<%= escape_javascript(render @task) %>");

